# Minimal mackup config: only the essentials that are NOT already symlinked
# from the dotfiles repo (git, zsh, vim, zed, ghostty config live in dotfiles).

[storage]
engine = dropbox

# Only sync these apps; nothing else.
[applications_to_sync]
ssh
composer
npm
opencode

[applications_to_ignore]
mackup
